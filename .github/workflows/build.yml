name: VS 2026

on:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron: 0 4 * * WED
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-2025-vs2026

    strategy:
      matrix:
        configuration: ["release", "debug"]
        platform: ["win32", "x64"]

    steps:
      - uses: reupen/github-actions/.github/actions/prepare-msbuild-vcpkg@v1
        name: Prepare MSBuild and vcpkg

      - name: Build component
        run: |
          msbuild /m '/p:Platform=${{ matrix.platform }};Configuration=${{ matrix.configuration }}' "/logger:$Env:Temp\Reupen.MSBuild.GitHubLogger.dll" vc18\console_panel.sln

      - uses: actions/upload-artifact@v7
        name: Upload component artefact
        if: matrix.configuration == 'release'
        with:
          name: Component package (${{ matrix.configuration }}, ${{ matrix.platform }})
          path: vc18\release-*\*.fb2k-component

      - uses: actions/upload-artifact@v7
        name: Upload symbols artefact
        if: matrix.configuration == 'release'
        with:
          name: Symbols for debugging (${{ matrix.configuration }}, ${{ matrix.platform }})
          path: vc18\release-*\*.pdb

  test:
    runs-on: windows-2025-vs2026

    strategy:
      matrix:
        configuration: ["debug"]
        platform: ["win32", "x64"]

    steps:
      - uses: reupen/github-actions/.github/actions/prepare-msbuild-vcpkg@v1
        name: Prepare MSBuild and vcpkg

      - name: Build tests
        run: |
          msbuild /m '/p:Platform=${{ matrix.platform }};Configuration=${{ matrix.configuration }}-test' "/logger:$Env:Temp\Reupen.MSBuild.GitHubLogger.dll" vc18\console_panel.sln

      - name: Run tests
        run: |
          Invoke-Expression '.\vc18\${{ matrix.configuration }}-${{ matrix.platform }}-v145\tests.exe'
